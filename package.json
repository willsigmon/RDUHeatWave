{
  "name": "rduheatwave",
  "version": "1.0.0",
  "private": true,
  "description": "RDU HeatWave \u2014 212 Referral Network chapter site",
  "scripts": {
    "check:json": "node scripts/check-json.mjs",
    "check:syntax": "node scripts/check-syntax.mjs",
    "check": "npm run check:syntax && npm run check:json && npm run test",
    "ci": "npm run check",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^3.1.1"
  },
  "dependencies": {
    "@vercel/og": "^0.11.1"
  }
}
